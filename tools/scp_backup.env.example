# SCP Backup Configuration
# Copy this file to 'scp_backup.env' and fill in your values
# Never commit the actual scp_backup.env file to version control!

# Remote SSH connection details
REMOTE_USER=your_ssh_username
REMOTE_HOST=your.remote.server.com
REMOTE_PORT=22
REMOTE_PATH=/path/to/remote/backup/directory

# SSH key for authentication (defaults to ~/.ssh/id_rsa if not specified)
SSH_KEY=/Users/${USER}/.ssh/id_ed25519

# Source directory (auto-detected if not specified)
WILLOW_DIR=/Volumes/1TB_DAVINCI/docker/willow

# Optional bandwidth limit in kilobits per second (0 = unlimited)
SCP_BW_LIMIT_KBIT_S=0

# Optional: Override archive and log directories
# ARCHIVE_DIR=/custom/path/to/archives
# LOG_DIR=/custom/path/to/logs